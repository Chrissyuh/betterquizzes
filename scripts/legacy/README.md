Legacy Guard Scripts
====================

These scripts are preserved as historical evidence from earlier BetterQuizzes
stages. They assert stale bundle names, route helper names, or stage markers and
are not part of the maintained verification surface.

Use the root-level scripts wired through `npm run verify` for current checks.
