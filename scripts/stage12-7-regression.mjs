#!/usr/bin/env node
// Compatibility wrapper: V1 intentionally replaces the old 12.7 checks
// where the question count label and arrow-only ordering were expected.
await import("./v1-regression.mjs");
