# BetterQuizzes versioning

Public app version: **BetterQuizzes V1**

The npm package uses `"version": "1.0.0"` because `package.json` must use a valid semantic version. The user-facing app, manifests, widget descriptions, resource URI, diagnostics, and documentation use **V1**.
